// using UnityEngine;
// using System;
// using System.Collections;
// using System.Collections.Generic;
// using System.Linq;

// namespace UnityLisp {
	// public class quote : block {
		
		// public quote(symbol symbol) {
			
			// type = new object_type("symbol");
		// }
		
		// public quote(block quoted) {
			// element = quoted.element;
			
			// type = new object_type("block");
		// }
		
		// public override string evalue() {
			
			// return "("+String.Join(" ", element.Select(e=>e.evalue()).ToArray())+")";
		// }
		
		// public override string to_string() {
			// return "[" + String.Join(", ", element.Select(e=>e.to_string()).ToArray()) + "]";
		// }
		
	// }
// }
